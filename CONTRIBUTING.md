# Contributing

1. Keep `SKILL.md` vendor-neutral.
2. Put platform-specific loading instructions only in `adapters/`.
3. Do not weaken read-only, evidence, conflict, or traceability requirements.
4. Update schemas when changing canonical JSON contracts.
5. Add tests for validation behavior.
