# GitHub Copilot Coding Agent Adapter

Add a repository instruction pointing to the canonical skill:

```text
Domain discovery tasks must follow skills/domain-rules-discovery/SKILL.md. Do not implement fixes. Produce the complete domain-discovery package with evidence and schema validation.
```
