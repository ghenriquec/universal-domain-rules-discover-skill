# Gemini CLI Adapter

Reference the canonical protocol from the repository's Gemini instruction file:

```text
Use skills/domain-rules-discovery/SKILL.md as the mandatory protocol for domain discovery. Work read-only and create only domain-discovery outputs.
```
