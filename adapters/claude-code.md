# Claude Code Adapter

Reference the canonical protocol from `CLAUDE.md`:

```text
When asked to discover business or domain rules, read skills/domain-rules-discovery/SKILL.md in full. The protocol is read-only and its output contract is mandatory.
```

The same repository can also be exposed as a Claude command or subagent prompt. Do not duplicate the canonical rules; link to `SKILL.md` to avoid drift.
