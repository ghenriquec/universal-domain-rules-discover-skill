# Cursor Adapter

Create a project rule that points to the canonical file:

```text
For domain-rule discovery and QA preparation, follow @skills/domain-rules-discovery/SKILL.md. Never modify product code during this task. Generate the required domain-discovery artifacts and validate their JSON schemas.
```
